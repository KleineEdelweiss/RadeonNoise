defmodule RadeonNoise.Sisters.Sister.Mem do
	# This submodule is for the GPU memory
	# functions of the Sister (card) module
	
	# VRAM amount
	# File: device/mem_info_vram_total
	def mem_total(dir) do
		
	end
	
	# Read VRAM usage
	# File: device/mem_busy_percent
	def mem_use(dir) do
		
	end
	
	# Read the VRAM speed
	# File: device/pp_dpm_mclk
	def mem_speed(f) do
		
	end
end